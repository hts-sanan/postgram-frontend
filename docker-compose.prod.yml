# Production-style build, served by nginx. Mirrors what actually ships.
# Usage:
#   docker compose -f docker-compose.prod.yml up --build
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    ports:
      - '8080:80'
    restart: unless-stopped
